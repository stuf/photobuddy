<Page ux:Class="HomePage">
	<Router ux:Dependency="router" />

	<JavaScript File="HomePage.js" />

	<DockPanel>
		<Text Color="White" FontSize="20" TextAlignment="Center" Dock="Top" Margin="0, 30">
			Recent entries
		</Text>
		<ScrollView>
			<StackPanel>
				<Rectangle ux:Class="Separator" Height="1" Fill="#fff2" />

				<Each Items="{entries}">
					<Separator />
					<Panel HitTestMode="LocalBoundsAndChildren" Clicked="{goToEntry}">
						<c.Text Value="{name}" Margin="20" />

						<WhilePressed>
							<Scale Factor=".95" Duration=".07" Easing="QuadraticOut" />
						</WhilePressed>
					</Panel>
				</Each>

				<Separator />
			</StackPanel>
		</ScrollView>
	</DockPanel>
</Page>